GT1 LABS — BUỔI 1
==================
1) GT1_Lab0_x_tien_toi.html
   - Chạm/vuốt để thấy x→a-, x→a+, x→+∞, x→-∞ là các quá trình.

2) GT1_Lab1_lo_tren_do_thi.html
   - f(x)=(x^2-1)/(x-1), x≠1.
   - Vuốt x→1 từ trái/phải; quan sát f(x)→2 dù f(1) không xác định.

3) GT1_Lab2_gioi_han_mot_phia.html
   - Vuốt x→0 từ trái/phải.
   - Thấy giới hạn trái 1, phải 3 ⇒ giới hạn hai phía không tồn tại.

CÁCH DÙNG
- Mở trực tiếp file .html trên máy/điện thoại.
- Để SV truy cập bằng QR: cần đưa file lên một địa chỉ web tĩnh/public trước (ví dụ Teams/SharePoint web hosting phù hợp, GitHub Pages, Cloudflare Pages, hoặc host tĩnh khác), rồi tạo QR từ URL đó.
- Các lab là HTML/JS tự chứa, không cần cài Python trên điện thoại. Python chỉ được dùng ở phía tạo/sản xuất lab.
